
# 🤖 认知图谱智能编辑器

> **问答即生成动态认知树** - Python后端 + React前端的全栈AI认知图谱系统

[![React](https://img.shields.io/badge/React-18.2.3-blue.svg)](https://reactjs.org/)
[![FastAPI](https://img.shields.io/badge/FastAPI-0.104.1-green.svg)](https://fastapi.tiangolo.com/)
[![WebSocket](https://img.shields.io/badge/WebSocket-Real--time-orange.svg)]()
[![AI](https://img.shields.io/badge/AI-DeepSeek-purple.svg)]()

## ✨ 项目特色

- 🎯 **智能拆解**: AI自动将复杂问题拆解为认知节点树
- 🎨 **可视化编辑**: 层次布局 + 曲线连接 + 交互式操作
- ⚡ **实时协作**: WebSocket双向通信，操作即时同步
- 🛠️ **功能丰富**: 节点增删改、路径导航、键盘快捷键
- 🎪 **美观界面**: 现代化UI设计，响应式布局

## 🚀 快速启动

### 方式一：一键启动（推荐）

```bash
# 双击项目根目录的 start.bat 文件
start.bat
```

### 方式二：手动启动

#### 1. 启动后端服务
```bash
cd backend
python -m venv .venv
.\.venv\Scripts\activate  # Windows
pip install -r requirements.txt
pip install openai tenacity transformers uvicorn
uvicorn main:app --host 0.0.0.0 --port 8001 --reload
```

#### 2. 启动前端服务
```bash
cd frontend
npm install
npm run dev -- --host 0.0.0.0 --port 5173
```

#### 3. 访问应用
```
http://localhost:5173
```

## 📋 环境要求

| 组件 | 版本要求 | 说明 |
|------|----------|------|
| Python | 3.8+ | 后端运行环境 |
| Node.js | 16+ | 前端构建工具 |
| npm/yarn | 7+ | 包管理器 |

## 🏗️ 项目结构

```
think_compass/
├── backend/                 # Python后端
│   ├── main.py             # FastAPI应用入口
│   ├── cognitive_graph.py  # 图数据结构
│   ├── gpt_agent.py       # AI代理逻辑
│   ├── prompts.py         # AI提示词配置
│   ├── chatgpt.py         # OpenAI客户端封装
│   ├── requirements.txt    # Python依赖
│   └── test_client.py     # 测试脚本
├── frontend/               # React前端
│   ├── src/
│   │   ├── main.jsx       # 应用入口
│   │   ├── App.jsx        # 主组件
│   │   └── components/
│   │       └── CognitiveGraphChat.jsx  # 核心聊天组件
│   ├── package.json       # Node.js依赖
│   └── vite.config.js     # Vite配置
├── start.bat              # Windows一键启动脚本
├── start.ps1              # PowerShell启动脚本
└── README.md              # 项目文档
```

## 🎮 使用指南

### 基础操作

1. **提问**: 在左侧输入框输入问题，点击发送
2. **查看拆解**: AI自动生成认知节点树
3. **导航**: 点击任意节点设为当前焦点
4. **路径**: 左上角显示当前认知路径

### 高级编辑

#### 鼠标操作
- **左键点击**: 选择节点
- **双击节点**: 编辑节点标签
- **右键菜单**:
  - 🎯 设为当前节点
  - ➕ 添加子节点
  - ✏️ 编辑节点
  - 🗑️ 删除节点

#### 键盘快捷键
- `Ctrl + N`: 添加新节点
- `Ctrl + Delete`: 删除选中节点
- `Enter`: 保存编辑
- `Escape`: 取消编辑

### 界面说明

```
┌─────────────────────────────────────┐
│ 当前认知位置：ROOT 》 A 》 A1       │ ← 路径导航
├─────────────────────────────────────┤
│ 消息历史记录...                     │ ← 聊天记录
│ [输入框] [发送]                      │ ← 发送区域
└─────────────────────────────────────┘
          │
          ▼
┌─────────────────────────────────────┐
│           认知图谱可视化             │ ← ReactFlow画布
│                                     │
│  • ROOT问题  ──────▶ • 子节点A     │
│     │                              │
│     └────────────▶ • 子节点B     │
│                                    │
│ [控制面板] [小地图]                 │ ← 控制组件
└─────────────────────────────────────┘
```

## ⚙️ 配置说明

### AI模型配置

在 `backend/main.py` 中配置：

```python
client = GPTClient(
    api_key="your-api-key",                    # API密钥
    model_or_deployment="deepseek-v3-1-250821", # 模型名称
    mode="openai",                             # 接口模式
    api_base="https://ark.cn-beijing.volces.com/api/v3/",  # 接口地址
)
```

### 端口配置

- **前端**: 默认5173端口，可在 `frontend/vite.config.js` 修改
- **后端**: 默认8001端口，可在启动命令中修改

## 🔧 开发指南

### 后端开发

```bash
cd backend
# 激活虚拟环境
.\.venv\Scripts\activate
# 安装开发依赖
pip install pytest black flake8
# 运行测试
pytest
```

### 前端开发

```bash
cd frontend
# 安装依赖
npm install
# 开发模式
npm run dev
# 构建生产版本
npm run build
```

## 📚 API文档

启动后端服务后，访问：
```
http://localhost:8001/docs
```

查看完整的API文档和WebSocket接口说明。

## 🚨 常见问题

### Q: 前端无法连接后端？
**A**: 检查端口是否被占用，确认后端服务已启动。

### Q: AI无法生成节点？
**A**: 检查API密钥是否正确，网络连接是否正常。

### Q: 节点布局混乱？
**A**: 点击右上角的"重新布局"按钮整理布局。

### Q: WebSocket连接失败？
**A**: 确认后端在8001端口运行，前端WebSocket地址正确。

## 🤝 贡献指南

1. Fork本项目
2. 创建功能分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 创建Pull Request

## 📄 许可证

本项目采用MIT许可证 - 查看 [LICENSE](LICENSE) 文件了解详情。

## 🙋‍♂️ 支持

如果遇到问题，请：

1. 查看[常见问题](#常见问题)部分
2. 在GitHub Issues中提交问题
3. 提供详细的错误信息和环境描述

---

**🎉 享受你的AI认知图谱编辑体验吧！**

